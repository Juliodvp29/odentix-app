# The tree was originally resolved this way: openapi-typescript@7 declares a
# typescript ^5 peer, but Angular 22 requires typescript ^6, so strict
# resolution (ERESOLVE) fails on any install. A single typescript@6 at the
# root is used for everything.
legacy-peer-deps=true
