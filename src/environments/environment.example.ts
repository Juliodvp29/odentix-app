// Template with placeholder values. Copy this file to
// environment.ts and environment.development.ts and fill in the real values.
// This file is committed; the real ones are git-ignored and never committed.
export const environment = {
  apiUrl: 'http://localhost:8081/api/v1',
};
