# shared

Reusable, feature-agnostic building blocks: UI components (buttons,
modals, tables, alerts, skeletons), pipes, and directives. Anything here
must work in any feature without modification — no business logic, no
imports from `features/` or feature-specific services.
