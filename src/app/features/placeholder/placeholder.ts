import { Component } from '@angular/core';

// Temporary scaffolding to prove lazy-loaded routing from the start.
// Removed once the first real feature lands.
@Component({
  selector: 'app-placeholder',
  template: '<p>Placeholder route. Feature modules will live here.</p>',
})
export class Placeholder {}
